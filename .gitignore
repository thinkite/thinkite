node_modules
dist
dist-electron
.expo
*.log
.DS_Store
.env*
!.env.example

# TS incremental build cache (emitted next to tsconfig.json, not inside dist/)
*.tsbuildinfo

# vitest / test coverage output
coverage

# Local Claude Code per-repo state (settings, agent worktrees)
.claude/

# `wrangler types` generated env + workerd runtime types (re-derive via cf-typegen)
# 13k+ lines of workerd type definitions — regenerable, not worth diffing.
worker-configuration.d.ts

# wrangler local dev state (.dev.vars, .wrangler/state caches)
.wrangler