{
  "name": "@sidecodeapp/sidecode",
  "version": "0.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "npm run typecheck --prefix packages/protocol && npm run typecheck --prefix packages/daemon && npm run typecheck --prefix packages/desktop && tsc --noEmit -p packages/app",
    "build": "npm run build --prefix packages/protocol && npm run build --prefix packages/daemon",
    "lint": "biome check .",
    "format": "biome check --write ."
  },
  "keywords": [],
  "author": "",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
