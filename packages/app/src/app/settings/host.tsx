/**
 * Android / web fallback. V0 is iOS-only — the real screen lives in
 * `host.ios.tsx`. See `index.tsx` for why this stub is needed.
 */
export default function SettingsHostScreen() {
  return null;
}
